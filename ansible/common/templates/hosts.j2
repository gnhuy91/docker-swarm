{% for host in groups['swarm'] %}
{% if hostvars[host]['ansible_eth1']['active'] and ansible_hostname != hostvars[host]['ansible_hostname'] %}
{{ hostvars[host]['ansible_eth1']['ipv4']['address'] }} {{ hostvars[host]['ansible_hostname'] }}
{% endif %}
{% endfor %}
